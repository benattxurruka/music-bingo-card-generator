# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Spotify app Client ID (developer.spotify.com → your app → Client ID)
# Redirect URI to register: http://localhost:3000/ (local) and your GitHub Pages URL (production)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
